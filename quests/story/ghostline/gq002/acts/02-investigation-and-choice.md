# Act II: Investigation And Choice

V scans the tenant classifier, clinic telemetry bridge, and failover invoice,
then reads the recovered exchange. Security intervenes before Cinder presents
the destroy-or-spoof decision.

Stages: `investigate_relay`, `read_hostage_circuit`, `return_to_relay`,
`relay_security`, `relay_decision`, `relay_choice`, and `operate_relay`.

The act exits with exactly one relay outcome selected and performed.
