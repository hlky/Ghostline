# Black Lantern Authoring Plan

## Authoring Order

1. Select and runtime-review the five principal world origins. Kabuki
   `drop_point_009` is the current runtime-confirmed provisional delivery target.
2. **Manifest-complete:** Mara has a stable provisional character record and
   validated isolated generation. Review her face/outfit before promotion;
   community lifecycle and navmesh route remain world-dependent.
3. **Complete:** `quests/story/ghostline/gq003/implementation/quest.json`
   records all 36 stages with unknown NodeRefs and assets explicitly left
   planned rather than presented as shipping-ready resources.
4. **Complete:** the generated journal contains all 28 objective phases, 23
   quest mappins, three contacts, ten phone conversations, and five
   readable/computer entries; quest-owned onscreen localization is authored.
5. **Dialogue-authoring complete:** eight manifests cover 128 spoken lines
   across all six acts, and every manifest has generated subtitles, subtitle
   maps, and VO maps. Both Iris manifests additionally have scene specs and
   scene CR2W-JSON. Wire the six non-scene delivery sets after world selection;
   record/render WEMs only after final performance review.
6. **Compiler-complete:** the freight-yard root runs its stealth monitor beside
   the ordered yard objectives and joins both branches. Runtime-prove its
   stop/success behavior after world placement.
7. **Compiler-complete:** rescue, escort, and the failure-blocking checkpointed
   defense are wired. Author the encounter signals and safe-site staging, then
   verify automatic reload in game.
8. The vehicle-chain graphs are compiler-complete. Select the interchange and
   relay routes, author the Patch van/carrier records and world placements, and
   verify cleanup removes only Patch's temporary vehicle.
9. **Authoring-complete, runtime-pending:** the multi-contact argument/debrief,
   outcome-dependent core/delivery, journal/localization, and item/reward
   records are authored. Build the core device placement and verify both routes.
10. Run compiler validation, scene validation, localization coverage checks,
    WolvenKit round trips, payload verification, and an in-game run from a
    pre-Ghostline save before beginning voice production.
